# Presentation Builder

All agent instructions for this repo live in **`AGENTS.md`** — your job
description, the house style, how to build and review decks, and the
project profile. **Read `AGENTS.md` now and follow it**, including the
first-run onboarding block at the top if it's still there.

`AGENTS.md` is the single source of truth so this kit works the same with
any coding agent (Claude Code, Codex, and others). Don't duplicate its
content here — keep edits in `AGENTS.md`.
